# Vista Hermosa · WFM Sales Dashboard

Streamlit dashboard for Whole Foods Market weekly sales.

## Run locally
```
pip install -r requirements.txt
streamlit run vh_dashboard.py
```

## Updating the data
Replace `VH_WFM_Item_Sales_Combined.csv` (and any dim_*.csv that changed),
commit, and push. The live app redeploys automatically.
